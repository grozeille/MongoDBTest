function Map() {
    /* 
    var key   = your grouping criterion;
    var value = intermediate state data associated with key; //a document
    emit(key, {value}); 
    */

    emit(this.Firstname, { count: 1, double1: this.Double1 });
}